VITE_API_URL=http://localhost:7001/api
# Web Push: the VAPID PUBLIC key (safe to expose — it ships in the client bundle).
# Must match VAPID_PUBLIC_KEY on the backend. Set the same value in Vercel.
VITE_VAPID_PUBLIC_KEY=your_vapid_public_key
